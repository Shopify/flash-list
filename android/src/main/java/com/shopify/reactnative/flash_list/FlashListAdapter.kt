package com.shopify.reactnative.flash_list

/**
 * Placeholder for future RecyclerView-based adapter.
 * Currently the NativeFlashListView uses a ScrollView-based approach
 * where React manages the child views directly.
 */
class FlashListAdapter {
    // Reserved for future RecyclerView implementation
}
